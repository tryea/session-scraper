# Credentials for login.py. Never commit the filled copy.
LOGIN_USERNAME=
LOGIN_PASSWORD=

# Optional: alerts go to Telegram when both are set, otherwise to the fallback file in the config.
MONITOR_TELEGRAM_TOKEN=
MONITOR_TELEGRAM_CHAT=
